# Generate with: node scripts/hash-pin.mjs 1234
NEXT_PUBLIC_FAMILY_PIN_HASH=sha256:replace_with_hash
# Optional pepper: if you use it in hash generation, set it here too
NEXT_PUBLIC_FAMILY_PIN_PEPPER=
# Optional override for GitHub Pages base path. Example: /my-repo
NEXT_PUBLIC_BASE_PATH=
